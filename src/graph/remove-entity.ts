/**
 * Remove an entity from the graph
 *
 * @param id - The ID of the entity to remove
 * @returns The operations to remove the entity
 */
export const removeEntity = async ({ id }: { id: string }) => {};
